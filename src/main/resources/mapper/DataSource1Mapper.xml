<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.xxx.application.infrastructure.dao.mysql.DataSource1TestDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xxx.xxx.application.domain.model.User">
        <id column="id" property="id" />
        <result column="name" property="name" />
    </resultMap>

	
	<!-- 用户角色表 列表查询  -->
    <select id="getUser" resultType="com.xxx.xxx.application.domain.model.User">
        SELECT id, name  FROM tb_user
        <where>
                    
        </where>
	</select>
	
</mapper>
